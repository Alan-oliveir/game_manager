pub struct ToolDefinition {
    pub bin_name: &'static str,       // "legendary"
    pub version_arg: &'static str,    // "--version"
}

// pub fn definition_for(tool: &ManagedTool) -> ToolDefinition { .. }